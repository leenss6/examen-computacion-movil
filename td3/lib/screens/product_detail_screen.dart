import 'package:flutter/material.dart';

class ProductDetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pantalla Detalles del Producto'), // Aquí puedes agregar el contenido de los detalles del producto
    );
  }
}
